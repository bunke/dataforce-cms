# DataForce CMS — local secrets. Copy to .env and fill in.
# This file is committed as .env.example; the real .env must be git-ignored.

DF_DB_HOST=localhost
DF_DB_NAME=CHANGE_ME
DF_DB_USER=CHANGE_ME
DF_DB_PASSWORD=CHANGE_ME
